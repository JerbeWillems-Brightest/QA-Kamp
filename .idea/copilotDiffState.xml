<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/Frontend/QA-Kamp/src/components/minigames/bugcleanup/BugCleanupGame.css">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Frontend/QA-Kamp/src/components/minigames/bugcleanup/BugCleanupGame.css" />
              <option name="updatedContent" value=".bugcleanup-root { font-family: var(--font-family, Arial, sans-serif); }&#10;.pz-layout .topbar { display:flex; align-items:center; justify-content:space-between; padding:8px 12px; background:#f5f7fa; border-bottom:1px solid #e1e6ef }&#10;.pz-layout .topbar .left { font-weight:600 }&#10;.pz-layout .topbar .center { font-weight:700 }&#10;.pz-layout .topbar .right button { margin-left:8px }&#10;&#10;.game-area { position:relative; height:420px; background:linear-gradient(#fff,#f3f6fb); overflow:hidden }&#10;.game-area .overlay { position:absolute; left:8px; top:8px; background:rgba(255,255,255,0.7); padding:6px 8px; border-radius:6px }&#10;&#10;.bug { position:absolute; background:radial-gradient(circle at 30% 30%, #ff5f5f 0%, #d94343 60%); border-radius:8px; box-shadow: 0 2px 6px rgba(0,0,0,0.2); cursor:pointer; transition:transform 0.08s ease }&#10;.bug:active { transform: scale(0.92) }&#10;&#10;.lag-cursor { position:absolute; width:18px; height:18px; border-radius:50%; background:#2b6bf6; box-shadow: 0 0 8px rgba(43,107,246,0.6); pointer-events:none; transform:translate(-9px,-9px) }&#10;&#10;.modal { position:absolute; inset:0; display:flex; align-items:center; justify-content:center; background:rgba(0,0,0,0.35) }&#10;.modal-card { background:#fff; padding:18px; border-radius:8px; width:360px; box-shadow:0 6px 20px rgba(0,0,0,0.2) }&#10;.modal-actions { margin-top:12px; display:flex; justify-content:flex-end }&#10;.modal-actions button { margin-left:8px }&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Frontend/QA-Kamp/src/components/minigames/bugcleanup/BugCleanupGame.tsx">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Frontend/QA-Kamp/src/components/minigames/bugcleanup/BugCleanupGame.tsx" />
              <option name="updatedContent" value="import React, { useEffect, useRef, useState } from &quot;react&quot;;&#10;import './BugCleanupGame.css';&#10;&#10;type AgeGroup = '8-10' | '11-13' | '14-16'&#10;&#10;interface Props {&#10;  ageGroup?: AgeGroup&#10;  onEnd?: (results: { score: number; timeMs: number; mistakes: number }) =&gt; void&#10;}&#10;&#10;type Bug = { id: number; x: number; y: number; size: number }&#10;&#10;const AGE_CONFIG: Record&lt;AgeGroup, { bugs: number }&gt; = {&#10;  '8-10': { bugs: 15 },&#10;  '11-13': { bugs: 25 },&#10;  '14-16': { bugs: 30 }&#10;}&#10;&#10;export default function BugCleanupGame({ ageGroup = '11-13', onEnd }: Props) {&#10;  // Game state&#10;  const [running, setRunning] = useState(false)&#10;  const [paused, setPaused] = useState(false)&#10;  const [showTutorial, setShowTutorial] = useState(true)&#10;  const [score, setScore] = useState(0)&#10;  const [mistakes, setMistakes] = useState(0)&#10;  const [bugs, setBugs] = useState&lt;Bug[]&gt;([])&#10;&#10;  // Timer&#10;  const startTimeRef = useRef&lt;number | null&gt;(null)&#10;  const [elapsedMs, setElapsedMs] = useState(0)&#10;  const rafRef = useRef&lt;number | null&gt;(null)&#10;&#10;  // Cursor lag simulation&#10;  const mouseRef = useRef({ x: 0, y: 0 })&#10;  const cursorRef = useRef({ x: 0, y: 0 })&#10;  const speedRef = useRef(0.12) // interpolation speed, increases when bugs removed&#10;  const containerRef = useRef&lt;HTMLDivElement | null&gt;(null)&#10;&#10;  // Penalty thresholds (ms) depending on age group&#10;  const penaltyThresholds = ageGroup === '8-10' ? [120_000, 180_000] : [90_000, 120_000, 150_000, 180_000, 210_000]&#10;  const appliedPenaltiesRef = useRef&lt;Record&lt;number, boolean&gt;&gt;({})&#10;&#10;  useEffect(() =&gt; {&#10;    // Initial spawn&#10;    const cfg = AGE_CONFIG[ageGroup]&#10;    setBugs(spawnBugs(cfg.bugs))&#10;    // focus for mouse events&#10;    const onMove = (e: MouseEvent) =&gt; {&#10;      const rect = containerRef.current?.getBoundingClientRect()&#10;      if (!rect) return&#10;      mouseRef.current.x = e.clientX - rect.left&#10;      mouseRef.current.y = e.clientY - rect.top&#10;    }&#10;    window.addEventListener('mousemove', onMove)&#10;    return () =&gt; window.removeEventListener('mousemove', onMove)&#10;  }, [ageGroup])&#10;&#10;  useEffect(() =&gt; {&#10;    if (running &amp;&amp; !paused) {&#10;      if (startTimeRef.current == null) startTimeRef.current = Date.now()&#10;      const loop = () =&gt; {&#10;        // update interpolation cursor&#10;        cursorRef.current.x += (mouseRef.current.x - cursorRef.current.x) * speedRef.current&#10;        cursorRef.current.y += (mouseRef.current.y - cursorRef.current.y) * speedRef.current&#10;        setElapsedMs(Date.now() - (startTimeRef.current || Date.now()))&#10;        checkPenalties()&#10;        rafRef.current = requestAnimationFrame(loop)&#10;      }&#10;      rafRef.current = requestAnimationFrame(loop)&#10;      return () =&gt; { if (rafRef.current) cancelAnimationFrame(rafRef.current); rafRef.current = null }&#10;    }&#10;  }, [running, paused])&#10;&#10;  function checkPenalties() {&#10;    const elapsed = Date.now() - (startTimeRef.current || Date.now())&#10;    penaltyThresholds.forEach((t, idx) =&gt; {&#10;      if (elapsed &gt;= t &amp;&amp; !appliedPenaltiesRef.current[idx]) {&#10;        appliedPenaltiesRef.current[idx] = true&#10;        setScore(s =&gt; Math.max(0, s - 2))&#10;      }&#10;    })&#10;  }&#10;&#10;  function spawnBugs(count: number) {&#10;    const list: Bug[] = []&#10;    for (let i = 0; i &lt; count; i++) {&#10;      list.push({ id: i + 1, x: 40 + Math.random() * 520, y: 40 + Math.random() * 320, size: (ageGroup === '8-10' ? 36 : ageGroup === '11-13' ? 28 : 20) })&#10;    }&#10;    return list&#10;  }&#10;&#10;  function startGame() {&#10;    setShowTutorial(false)&#10;    setRunning(true)&#10;    setPaused(false)&#10;    startTimeRef.current = Date.now()&#10;    setScore(0)&#10;    setMistakes(0)&#10;    appliedPenaltiesRef.current = {}&#10;  }&#10;&#10;  function handlePause() {&#10;    setPaused(p =&gt; !p)&#10;  }&#10;&#10;  function handleBugClick(bugId: number) {&#10;    // require that cursor overlap to count as hit - compute distance between interpolated cursor and bug&#10;    const b = bugs.find(x =&gt; x.id === bugId)&#10;    if (!b) return&#10;    const dx = cursorRef.current.x - b.x&#10;    const dy = cursorRef.current.y - b.y&#10;    const dist = Math.sqrt(dx * dx + dy * dy)&#10;    const hitRadius = b.size&#10;    if (dist &lt;= hitRadius + 8) {&#10;      // correct&#10;      setBugs(prev =&gt; prev.filter(x =&gt; x.id !== bugId))&#10;      setScore(s =&gt; s + 2)&#10;      // increase interpolation speed slightly (cursor becomes more responsive)&#10;      speedRef.current = Math.min(0.5, speedRef.current + 0.02)&#10;      // if no bugs left -&gt; end&#10;      setTimeout(() =&gt; {&#10;        if (bugs.length - 1 &lt;= 0) {&#10;          endGame()&#10;        }&#10;      }, 0)&#10;    } else {&#10;      // miss&#10;      setMistakes(m =&gt; m + 1)&#10;      setScore(s =&gt; Math.max(0, s - 0))&#10;    }&#10;  }&#10;&#10;  function endGame() {&#10;    setRunning(false)&#10;    if (onEnd) onEnd({ score, timeMs: elapsedMs, mistakes })&#10;  }&#10;&#10;  // simple mm:ss formatter&#10;  function formatMs(ms: number) {&#10;    const s = Math.floor(ms / 1000)&#10;    const mm = Math.floor(s / 60).toString().padStart(2, '0')&#10;    const ss = (s % 60).toString().padStart(2, '0')&#10;    return `${mm}:${ss}`&#10;  }&#10;&#10;  return (&#10;    &lt;div className=&quot;pz-layout bugcleanup-root&quot;&gt;&#10;      &lt;div className=&quot;topbar&quot;&gt;&#10;        &lt;div className=&quot;left&quot;&gt;Bug Cleanup&lt;/div&gt;&#10;        &lt;div className=&quot;center&quot;&gt;{running ? formatMs(elapsedMs) : '00:00'}&lt;/div&gt;&#10;        &lt;div className=&quot;right&quot;&gt;&#10;          &lt;button className=&quot;hint&quot;&gt;Hint&lt;/button&gt;&#10;          &lt;button className=&quot;pause&quot; onClick={handlePause}&gt;{paused ? 'Resume' : 'Pause'}&lt;/button&gt;&#10;        &lt;/div&gt;&#10;      &lt;/div&gt;&#10;&#10;      &lt;div className=&quot;game-area&quot; ref={containerRef}&gt;&#10;        {/* background / office visual could go here */}&#10;        &lt;div className=&quot;info overlay&quot;&gt;Score: {score} &amp;nbsp; Bugs: {bugs.length} &amp;nbsp; Mistakes: {mistakes}&lt;/div&gt;&#10;&#10;        {/* render bugs */}&#10;        {bugs.map(b =&gt; (&#10;          &lt;div&#10;            key={b.id}&#10;            className=&quot;bug&quot;&#10;            style={{ left: b.x - b.size / 2, top: b.y - b.size / 2, width: b.size, height: b.size }}&#10;            onClick={() =&gt; handleBugClick(b.id)}&#10;            role=&quot;button&quot;&#10;            aria-label={`bug-${b.id}`}&#10;          /&gt;&#10;        ))}&#10;&#10;        {/* laggy cursor visual */}&#10;        &lt;div&#10;          className=&quot;lag-cursor&quot;&#10;          style={{ transform: `translate(${cursorRef.current.x}px, ${cursorRef.current.y}px)` }}&#10;        /&gt;&#10;&#10;        {/* pause overlay */}&#10;        {paused &amp;&amp; (&#10;          &lt;div className=&quot;modal&quot;&gt;&#10;            &lt;div className=&quot;modal-card&quot;&gt;&#10;              &lt;h3&gt;Gepauzeerd&lt;/h3&gt;&#10;              &lt;div className=&quot;modal-actions&quot;&gt;&#10;                &lt;button onClick={() =&gt; setPaused(false)}&gt;Verder spelen&lt;/button&gt;&#10;                &lt;button onClick={() =&gt; { setRunning(false); setPaused(false); }}&gt;Stoppen&lt;/button&gt;&#10;              &lt;/div&gt;&#10;            &lt;/div&gt;&#10;          &lt;/div&gt;&#10;        )}&#10;&#10;        {/* tutorial */}&#10;        {showTutorial &amp;&amp; (&#10;          &lt;div className=&quot;modal&quot;&gt;&#10;            &lt;div className=&quot;modal-card&quot;&gt;&#10;              &lt;h3&gt;Even oefenen - Bug Cleanup&lt;/h3&gt;&#10;              &lt;p&gt;De muis reageert traag. Klik op bugs om ze te verwijderen. Elke bug maakt de muis iets sneller.&lt;/p&gt;&#10;              &lt;div className=&quot;modal-actions&quot;&gt;&#10;                &lt;button onClick={startGame}&gt;Start spel&lt;/button&gt;&#10;                &lt;button onClick={() =&gt; { setShowTutorial(false); setRunning(true); }}&gt;Sla over&lt;/button&gt;&#10;              &lt;/div&gt;&#10;            &lt;/div&gt;&#10;          &lt;/div&gt;&#10;        )}&#10;      &lt;/div&gt;&#10;    &lt;/div&gt;&#10;  )&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>